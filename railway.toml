# API service (voice + OCR + DB). In Railway: root directory = /
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c \"uv run --project bot uvicorn backend.server:app --host 0.0.0.0 --port $PORT\""
restartPolicyType = "ON_FAILURE"
