# Kiosk UI. In Railway: set this service Root Directory to /client
[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npx --yes serve -s dist -l tcp://0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
